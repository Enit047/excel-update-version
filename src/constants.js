export const defaultStyle = {
    textAlign: 'left',
    fontWeight: 'normal',
    fontStyle: 'normal',
    textDecoration: 'none'
}
export const tableName = 'New table'

export function preventMenu(e) {
    e.preventDefault()
}
